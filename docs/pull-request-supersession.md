# استبدال الطلبات السابقة بطلب التكامل #16

تحديث: 2026-09-25. رأس التكامل المفحوص: `71ac8df`.

هذه وثيقة **أدلة قبل قرار**، لا اقتراح إغلاق. القرار في إغلاق أي طلب للمالك، وموافقة GitHub خطوة منفصلة عن نجاح الفحوص.

## الخلاصة

| الطلب | الفرع | الرأس | commits خارج #16 | الحكم |
| --- | --- | --- | --- | --- |
| [#13](https://github.com/FMalki1406/FHD/pull/13) | `fix/independent-controls` | `87121d6` | **0** | **سلف مباشر لرأس #16** |
| [#14](https://github.com/FMalki1406/FHD/pull/14) | `fix/publication-recovery` | `ed258a1` | 1 | تغطيته محفوظة، بجدول مفصّل |
| [#15](https://github.com/FMalki1406/FHD/pull/15) | `feature/publish-from-handle` | `fbb10f0` | 1 | أثر ذلك الـcommit موجود في #16 |

## #13 — احتواء بالنَّسب

`git merge-base --is-ancestor` يؤكد أن رأسه سلف لرأس #16: كل commit فيه موجود حرفيًا في سلسلة التكامل، وصفر خارجها.

## #15 — تصحيح ادعاء سابق

قلتُ في تقرير سابق إن #15 «سلف مباشر». **هذا كان صحيحًا عند رأسه `224b9bf`، ولم يعد صحيحًا** — وأنا من أحدث الفارق: دفعتُ عليه `fbb10f0` لإصلاح عطله في نطاقه، بعد أن كان #16 قد تفرّع. فالفرع الآن يحمل commit واحدًا ليس في #16.

وفُحص أثره لا نَسَبه: ذلك الـcommit يضيف `#![cfg(windows)]` إلى جذر `crates/bins/daemon/tests/publication.rs`، **والسطر نفسه موجود في #16** (وصل إليه عبر مسار آخر، ضمن تشخيص فشل clippy على Unix). فلا شيء فيه مفقود.

## #14 — تغطية محفوظة، مفحوصة اختبارًا اختبارًا

الجدول التفصيلي في [عقد النشر](publication-contract.md): اختبار واحد كان مفقودًا فعلًا واستُعيد، وثلاثة أبطلها تغيير التصميم مع تحديد الخلَف الأقوى لكلٍّ منها.

**ولا يُدمج #14 بصيغته لمجرد أنه أخضر:** انحدار فك الختم الذي أثبتته المراجعة عليه عولج في #16، لا فيه.

## لماذا لا تُجعل الفروع القديمة خضراء

#13 ← #15 ← #16 ترتيبًا. و**#15 هو التغيير الذي يجعل النشر خاصًّا بـWindows**، بينما مجموعات التنزيل المتكامل والمقبس والبرنامج تفترض النشر على كل منصة. والعمل الذي يُخضع كلًّا منها لعقد منصتها هو **جوهر #16 نفسه**.

وهذا مقيس لا مستنتَج: بعد إصلاح عطل #15 في نطاقه، انتقل الفشل إلى الخطوة `Test engine end to end` على ubuntu وmacOS — [التشغيل 36109993697](https://github.com/FMalki1406/FHD/actions/runs/36109993697). فجعل #15 أخضر يساوي إعادة إنتاج #16 داخله.

**ونسخ الإصلاحات إلى ثلاثة فروع لتوحيد اللون مرفوض**: يغيّر نطاق كل طلب فلا يبقى «ذلك التغيير»، ويصنع فروعًا لا يدمجها أحد لأن دمج #16 يسلّم كل شيء.

## ما لا تغيّره هذه الوثيقة

خُضرة الفحوص **ليست جاهزية منتج ولا موافقة مراجعة**. وباقٍ كما هو:

- التنزيل **لا يكتمل** على Linux وmacOS.
- الإجراء العلاجي للتعطل بين الختم والرابط **غير منفَّذ**؛ ن٤ غير مكتمل.
- آلية النشر على Unix غير مقيسة.
- ضمان هوية الوجهة منذ اختيار المستخدم متطلب على تكامل الواجهة.
- **م٣ غير معتمد، وحظر الإصدار مستمر.**
