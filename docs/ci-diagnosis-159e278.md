# تشخيص CI — 159e278

التاريخ: 2026-09-24. نطاق هذه الجولة: قراءة سجل الفشل ومطابقته بالكود، لا إعادة اعتماد F1/F2/F3 أو مراجعة أمنية كاملة.

الرأس: `159e278734b85a462a4ec2310e289bfd943481fb`.
التشغيل: [36032905707](https://github.com/FMalki1406/FHD/actions/runs/36032905707).

## الفشل المثبت

[وظيفة Ubuntu](https://github.com/FMalki1406/FHD/actions/runs/36032905707/job/107745819836) — الخطوة 33، `Test engine end to end`.

```text
thread 'a_later_run_continues_what_it_remembers_without_being_told_the_link'
panicked at crates/bins/daemon/tests/end_to_end.rs:774:44:
called `Result::unwrap()` on an `Err` value:
Os { code: 2, kind: NotFound, message: "No such file or directory" }

test result: FAILED. 12 passed; 1 failed; 1 ignored
```

قرأت الرسالة مباشرة من سجل GitHub في المتصفح المسجل دخوله، ثم طابقتها بنص الملف عند SHA أعلاه.

## السبب

السطر 774 ما زال ينفذ على كل المنصات:

```rust
assert_eq!(std::fs::read(&destination).unwrap(), body);
```

لكن فرع Unix عند 761–770 كان قد أكد للتو `NeedsAction` وسبب `Storage`، وعدم وجود الوجهة، ووجود الجزء الكامل المطابق للجسم. ثم خرج من `match` إلى قراءة الوجهة غير الموجودة. وصول التنفيذ إلى السطر 774 يثبت أن تأكيدات ذلك الفرع سبقته ونجحت في هذا التشغيل.

إعادة الفتح عند 746 وتشغيل المهام عند 748 لم يكونا موضع الفشل. لا حاجة إلى تخمين خلل فيهما بناءً على هذا السجل.

## التصحيح المطلوب من المنفذ

حصر مقارنة محتوى الملف النهائي في مساري Windows المقبولين (`Published` و`Completed`) أو تحت شرط `PUBLISHES`. إبقاء إعادة الفتح والاستمرار وتأكيدات الجزء على Unix كلها منفذة. لا إعادة إضافة خروج مبكر قبل الاستمرار، ولا قبول خطأ قراءة الوجهة كتأكيد عام.

هذا التصحيح في الاختبار نفسه، ولا يحتاج خطافًا في كود الإنتاج أو محاكاة آلية Unix على Windows. يلزم CI على الرأس الجديد لإثبات النتيجة.

## حالة المنصات وقت الفحص

- Windows: ناجح للرأس المحدد.
- Ubuntu: فشل واحد كما أعلاه. اختبار الإيقاف الجديد واختبار استئناف الاتصال المنقطع ناجحان في هذا التشغيل؛ ذلك ليس إعادة مراجعة كفاية أدلتهما.
- macOS: `in_progress` في `Test engine end to end` وقت آخر استعلام. لا نتيجة نهائية ولا تشخيص مفترض له.

لم أعدل كود الإنتاج أو الاختبارات أو الفروع، ولم أدمج شيئًا. هذا الملف هو التغيير الوحيد لهذه الجولة.
