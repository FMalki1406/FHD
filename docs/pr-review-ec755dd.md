# مراجعة ec755dd وتوجيه عمل التعافي

التاريخ: 2026-09-25. الرأس `ec755dd29bc156567af8152061d6c534c43f68ef`.

## ما أُغلق

راجعت اختبار التحذيرات الجديد وشغّلته محليًا على Windows: ناجح. يغطي رفض الرمز غير المعلن وتجاوز العدد عند الترميز وفك الترميز، مع ضبط موجب لرد مشروع. الإطار غير المشروع JSON صحيح، فلا يعتمد الرفض على خطأ صياغة. تُغلق ملاحظة نقص هذه الاختبارات بنطاقها. لم أعد تجربة الطفرات بنفسي، فلا أنسب لنفسي إثباتها.

[CI 36113281374](https://github.com/FMalki1406/FHD/actions/runs/36113281374) ناجح على Windows وUbuntu وmacOS للرأس نفسه، وفق head_sha في API. هذا ليس اعتماد دعم النشر على Unix أو موافقة دمج شاملة.

## الوثائق والطلبات

إبقاء تقارير المراجعة الاثني عشر داخل docs مناسب لمتطلب المالك توثيق العمل. تبقى سجلات تاريخية مربوطة برؤوسها، ولا تُعدل نتائج الماضي لتبدو موافقة على الرأس الحالي. تُجمع الحالة الراهنة في مرجع واضح مستقل.

وثيقة الاستبدال تصحح نسب #15 وتربط جدول اختبارات #14 بعقد النشر. تبقى عبارة «جعل #15 أخضر يساوي إعادة إنتاج #16» أقوى من دليل انتقال الفشل: إصلاح فحوص فرع قديم ممكن بنطاق أضيق، والتوصية بتوحيد مسار الدمج هي لتجنب صيانة متوازية متكررة، لا استحالة تقنية مثبتة. يصح تصحيح العبارة دون تغيير قرار العمل على #16.

لم تُغلق الطلبات أو تُدمج في هذه الجولة، ولا يُعاد اعتماد #14 القديم بسبب خضرة فحوصه.

## أولوية المنفذ التالية: التعافي بين الختم والرابط

أولوية مناسبة ضمن العمل المعتمد. لكنها ليست دليلًا على أنه آخر بند سلامة في الميزة أو المشروع.

معايير القبول المطلوبة قبل اعتبار ن٤ مكتملًا:

1. فصل التعطل قبل إنشاء الرابط، وبعده وقبل تسجيل التسليم، وتعذر حسم ما حدث. لا يُستخدم غياب المسار وحده لإثبات أن الكائن لم يُنشر، خصوصًا مع انتقال المجلد أو تعذر قراءته.
2. لا رفع لختم كائن يُحتمل أنه سُلّم للمستخدم، ولا إعادة فتحه للكتابة. عند الغموض يُحافَظ عليه ويُستخدم مسار تعافٍ منفصل موثق؛ اختيار آليته مسؤولية التنفيذ والمراجعة، لا حسم افتراضي هنا.
3. استعادة التقدم الذي يمكن إثبات سلامته، وإظهار إجراء واضح للمستخدم عندما تلزم إعادة تنزيل أو تدخل. لا حذف أو استبدال ملف غير مملوك للمهمة، ولا إعادة نشر خفية.
4. اختبارات أعطال تعيد فتح الحالة من القرص عند الحدود المذكورة، وتشمل وجود الوجهة وتحركها وتكرار التعافي. يُتحقق من سلامة محتوى الملف الذي سُلّم سابقًا وعدم تغيره.
5. مراجعة Engineering وSecurity مستقلة للتصميم والتنفيذ؛ نتائج كل منصة تُعرض بنطاق دعمها الفعلي.

تبقى الملاحظة غير المانعة حول اختبار barrier المعتمد على التوقيت مسجلة في التقرير السابق؛ لا يعيد ذلك فتح إصلاح الحاجز الذي أُثبت مستقلًا. تبقى بقية متطلبات الميزة وسلامة المسارات ومصفوفة دعمها خاضعة لسجلها، ولا يصح شطبها بعبارة «آخر بند».

لم أغير الإنتاج أو الفروع ولم أدفع أو أدمج أو أسجل موافقة GitHub. أُضيف هذا التقرير فقط. حظر الإصدار وم٣ غير المعتمد باقيان.
