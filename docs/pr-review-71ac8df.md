# مراجعة 71ac8df وتنظيم الطلبات

التاريخ: 2026-09-25. الرأس `71ac8dfe8e92f9bdb04736b6915db15dd49d04f3`.

## النتيجة المحددة

R1 للحاجز مغلق بنطاق حالتي الاتصال المقبول قبل الانتظار وأثناءه. أعاد المراجع الهندسي التجربتين بترتيب حتمي على helper الرأس نفسه، وأثبت استمرار الانتظار حتى إكمال الطلب. التجربة في `target/review-quiet-71ac/src/main.rs` على Windows. شغّلت اختبار barrier الأصلي محليًا ونجح في 9.30 ثانية.

المراجعة الأمنية لم تثبت عيبًا جديدًا في فرق التحذيرات: مجموعة الرموز مغلقة، وحد العدد 8 مفروض عند الترميز وفك الترميز، والعرض يمر عبر printable. حد إطار 256 KiB السابق يقيد المدخل قبل تحليل JSON. اختبار الخدمة يشترط تحذير DESTINATION-SHARED غير فارغ عبر IPC؛ هذه تغطية ذات معنى.

لا يساوي ذلك اعتمادًا شاملًا لـPR أو ن٤ أو م٣ أو الإصدار.

## قيود التحقق والمتابعة

- لا اختبارات سلبية مضافة لرفض رمز مجهول أو تجاوز عدد التحذيرات؛ يلزم تضمينهما لإثبات حماية هذا الحد عند التغيير مستقبلًا، مع ضبط موجب لرد مشروع.
- اختبار الصلاحيات يثبت قرار التحذير ووصوله عبر IPC. لا يثبت وصول حساب آخر بالفعل، لأن مجلد الوجهة تحت شجرة اختبار محمية، ولا يختبر طباعة CLI نفسها.
- اختبار barrier الجديد يقرأ in_flight بعد عودة quiet؛ قبول اتصال لاحق بين الحدثين يمكن أن ينتج فشلًا كاذبًا. لم نعد إنتاج هذا الفشل عمليًا، ولا نعيد فتح R1 بسببه. يوصى باستخدام الترتيب الحتمي الذي أثبت الإصلاح بدل توزيع المحاولات بالتوقيت.

## تصحيح علاقة الطلبات

فحص git وGitHub API أكد الرؤوس التالية:

| الطلب | الرأس | علاقته بـ71ac8df |
| --- | --- | --- |
| #13 | 87121d6 | سلف مثبت |
| #15 | fbb10f0 | **ليس سلفًا**؛ التزامه الإضافي غير موجود في النسب |
| #14 | ed258a1 | ليس سلفًا؛ مطابقة النقل دلالية كما في المراجعات السابقة |

فرق #15 غير الموروث هو fbb10f0 فقط: إضافة cfg(windows) إلى ملف publication.rs. الأثر نفسه موجود أصلًا في #16، مع تعليق يشرح حدود اختبارات Windows. لذلك وصف «#15 الحالي سلف مباشر» غير صحيح، بينما القول إن **أثر التزامه الإضافي موجود** مؤيد بمقارنة المصدر. لم نفحص كل تاريخ #13 دلاليًا مجددًا في هذه الجولة.

## التوصية للمالك والمنفذ

استخدام #16 كمسار التكامل المرشح، وعدم إعادة إنتاجه كاملًا في فروع قديمة لمجرد تلوين القائمة. توثيق الطلبات المستبدلة وربطها بـ#16، ثم إغلاقها بصفتها مستبدلة بعد إتمام جدول المطابقة؛ لا يوصف الإغلاق بأنه نجاح فحوصها القديمة، ولا يدمج #14 القديم الأخضر رغم عيب فك الختم الذي أثبتته مراجعته.

الادعاء بأن إخضار #15 يستلزم حتمًا إعادة بناء #16 كله لم يُثبت؛ التوصية بالتوحيد تنظيمية لتجنب تكرار العمل، وليست حقيقة تقنية عن استحالة إصلاح فرع مستقل.

## أدلة CI وحدود العمل

[التشغيل 36111564505](https://github.com/FMalki1406/FHD/actions/runs/36111564505): الفحوص الثلاثة ناجحة للرأس أعلاه حسب API. لم نعتمد نجاح Unix دليلًا على اكتمال النشر عليه.

المراجعة الأمنية مصدرية مستقلة؛ تجربة الحاجز المستقلة وتشغيل الاختبار المحلي على Windows فقط. لا تغيير إنتاجي أو فرع أو دفع أو إغلاق أو دمج أو موافقة GitHub في هذه الجولة؛ أضيف التقرير فقط. تبقى حدود التعافي والنشر على Unix وهوية الوجهة منذ اختيارها قائمة كما أعلن المنفذ.
